# Kagaz BE-01 — Backend Setup

## Overview

This folder contains the initial FastAPI backend setup for the Kagaz project.

## Features

- FastAPI application
- Health check endpoint
- CORS configuration
- Basic test structure
- Project dependencies

## Run the Backend

Install dependencies:

```bash
python -m pip install -r requirements.txt